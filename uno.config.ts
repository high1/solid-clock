import { defineConfig } from 'unocss';
import { colors } from 'unocss/preset-mini';

export default defineConfig({
  theme: {
    colors: {
      gray: colors.zinc,
      solid: '#518ac8',
    },
  },
});
